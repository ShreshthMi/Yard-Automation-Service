# Placeholder Dockerfile for Java Backend
# Uncomment and modify once the Java code is added

ARG CI_JOB_TOKEN
ARG CI_API_V4_URL

# FROM eclipse-temurin:17-jdk-alpine AS build
# WORKDIR /app
# COPY Backend/ .
# RUN ./mvnw clean package -DskipTests

# FROM eclipse-temurin:17-jre-alpine
# WORKDIR /app
# COPY --from=build /app/target/*.jar app.jar
# EXPOSE 8080
# ENTRYPOINT ["java", "-jar", "app.jar"]

# For now, a simple placeholder to prevent build failure if referenced
FROM alpine
CMD ["echo", "Backend is not yet implemented"]
